# Cloudflare Pages _redirects
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/

# Redirect www to non-www (update YOUR_DOMAIN)
# https://www.raineyproperty.group/* https://raineyproperty.group/:splat 301

# No catch-all rewrite here: this site is a single index.html at /. A `/* /index.html 200`
# rule is rejected by Workers static assets (infinite loop with path normalization).
